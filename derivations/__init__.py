"""Symbolic derivations for unfolding weight identities (story 005).

Each module is runnable standalone (`python derivations/<name>.py`) and
importable (`run()` performs the assertions). LaTeX outputs are written to
``derivations/out/`` for reuse by the paper (Epic 5).
"""
